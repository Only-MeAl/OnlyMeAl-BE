<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onlymeal.domain.user.dao.UserDao">

    <insert id="insertUser">
        INSERT INTO users (email, password, nickname, gender, birth_date, height, weight, activity_level, target_goal)
        VALUES (#{email}, #{password}, #{nickname}, #{gender}, #{birthDate}, #{height}, #{weight}, #{activityLevel}, #{targetGoal})
    </insert>

    <select id="getUserByEmail" resultType="User">
        SELECT *
        FROM users
        WHERE email = #{email}
    </select>

    <select id="getUserById" resultType="User">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <update id="updateUser">
        UPDATE users
        <set>
            <if test="request.nickname != null">nickname = #{request.nickname},</if>
            <if test="request.nickname != null">nickname = #{request.nickname},</if>
            <if test="request.gender != null">gender = #{request.gender},</if>
            <if test="request.birthDate != null">birth_date = #{request.birthDate},</if>
            <if test="request.height != null">height = #{request.height},</if>
            <if test="request.weight != null">weight = #{request.weight},</if>
            <if test="request.activityLevel != null">activity_level = #{request.activityLevel},</if>
            <if test="request.targetGoal != null">target_goal = #{request.targetGoal},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <update id="updateCoach">
        UPDATE users
        SET coach_tone = #{coachTone},
            coach_personality = #{coachPersonality}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser">
        DELETE FROM users
        WHERE user_id = #{userId}
    </delete>
</mapper>